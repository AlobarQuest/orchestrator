---
name: orchestrator
tier: active
status: active
purpose: Canonical work-unit lifecycle control plane for the software factory.
version: 0.1.0
version_source: pyproject
updated: '2026-07-31'
delivery_profile: dependency-update
foundation: true
foundation_contract: 1
applicable_standards:
  project: '1.0'
  security: '1.0'
  code: '1.0'
required_checks:
- id: quality
  executor: github-actions:quality.yml
---

## Backlog

- [ ] (P2) Clean up 4 stale `bump-dependencies-*` work units in production (change-manager `3a650c23`, infraops-mcp-server `69e63f39`, security-standards `ddda84ac`, brain `4afb7207`), all in `ready` state from the original 2026-07-09 fanout, superseded by the rev-4..9 re-proofs. Each has `authority_approval_id: None`, `mutation_commands: None`, and the old `allowed_commands: ['make check']` — so they fail dispatch admission closed and cannot call GitHub, but they are debris that could be mistaken for a corrected replacement. **CORRECTED 2026-07-28: these units ARE retirable, and the "un-retireable" claim below was wrong.** The 2026-07-23 finding was accurate about the DIRECT edge and wrong about the conclusion: `(READY, CANCELLED)` is indeed not legal for any role, but `(READY, FAILED)` is a **SYSTEM** edge and `(FAILED, CANCELLED)` is a **HUMAN** edge (`kernel/transitions.py`), so the two-step path `ready → SYSTEM commands/fail → HUMAN /review cancel` retires them today with no code change. Proven in production by the 2026-07-27 drill run, which drove five drill units terminal by exactly this route. A `(READY, CANCELLED)` HUMAN edge remains **optional ergonomics** (one hop instead of two), not a prerequisite — do not gate the cleanup on it. Retirement runbook: `docs/operations/production-drill-adaptations.md`; being executed 2026-07-28 (gap-closure workplan GAP-3). Original finding 2026-07-22 during the AC-003 dispatch; cancel-path claim confirmed-then-corrected 2026-07-23 → 2026-07-28. — added 2026-07-22
- [ ] (P2) Silence is never approval — **half shipped, and the remaining half is deliberately narrow.** VERIFIED 2026-07-28 against code: WS-P2.15 shipped the *reporting* half. `dead_letter()`'s `_stalled_approvals` (`services/dead_letter.py`) reports every unit in `awaiting_approval` OR `awaiting_review` that has sat past `dead_letter_stalled_approval_seconds` as `approval_unanswered`, not requeue-eligible; the threshold is a plain `int` (default 7 days, capped at 30, **no off value**) precisely because its `int | None = None` predecessor is why the previous age-out sat invisible for a whole workstream. Exercised in production by drill 5 on 2026-07-27. **So "a unit can sit forever with nobody told" is CLOSED.** What remains open is only this: the entry is reported and never acted on — there are still no fail-closed timeout *semantics* (nothing transitions a stalled unit, by design: "a stalled gate needs a human decision, not a retry"). Decide whether that is the intended terminal answer — in which case close this item — or whether an unanswered gate should eventually auto-transition, which is a lifecycle-design question, not a reporting gap. Note `work_units.updated_at` cannot be back-dated (a DB trigger rewrites it), so any test of this must shrink the threshold, never age the row. — added 2026-07-12
- [x] (P1) A worker that never reports its PR binding is INVISIBLE to conflict detection: no binding -> the runner has no PR to poll -> no observation -> no condition, and not even a counted `skipped_correlation`. It also breaks the `work unit ↔ PR` link that the Wave-2 Evidence Pack (WS-P2.5) and traceability query (WS-P2.6) are built on. Fix fail-closed, not with a new detector: a unit whose authority includes PR-creating capability must not reach SUBMITTED without a `unit_pr_binding` row. **Scoped into WS-P2.15 (fail-closed lifecycle guards), Wave 1** — added 2026-07-12; DONE 2026-07-23 (WS-P2.16 U3: attempt-scoped EXECUTING→SUBMITTED binding guard, deployed on 4cfa0c8-wsp216-amd64)
- [x] (P1) Rotate the production orchestrator-postgres password — exposed in a Claude session transcript 2026-07-09 (internal-Coolify-network-only exposure); rotate DB password, update ORCHESTRATOR_DATABASE_URL in Coolify, redeploy, verify /health/ready — added 2026-07-09; DONE 2026-07-29 (GAP-7): `ALTER USER` on the live DB, both `ORCHESTRATOR_DATABASE_URL` scopes (production + preview) rewritten, app restarted, container env verified by hash. Old value proven dead (`old_password_auth=FAIL`), with a wrong-password control proving the probe discriminates. Pre- and post-rotation backups both verified restorable into a scratch DB (30 tables / 37 work_units / 576 events / head `0020_wsp28_follow_up`, identical). Consumer inventory established that the vps-backup lane is NOT a credential consumer (`pg_hba` is `local ... trust`, so `docker exec pg_dump` needs no password) and that there is no separate migration URL. Decision: the password stays Coolify-only, no BWS entry (matching `ORCHESTRATOR_CSRF_SECRET` / `ORCHESTRATOR_PROXY_MARKER`). Closeout: `~/docs/software-delivery-system/2026-07-29-gap7-postgres-password-rotation.md`
- [x] (P2) Update the Coolify DB-resource `postgres_password` field for `orchestrator-postgres` to the rotated value (manual UI step; `coolify_update_database` exposes no parameter for it) — added 2026-07-29; DONE 2026-07-29 by Devon in the Coolify UI, verified in-process by hash (stored value matches the live password; `internal_db_url` carries it). The save did NOT restart the DB container, which is correct — `POSTGRES_PASSWORD` is read only by `initdb` on an empty `PGDATA`, so a restart would not change what the DB accepts. All four consumers now agree.
- [ ] (P3) infraops escalation: `coolify_update_database` exposes no `postgres_password` parameter, so rotating a Coolify-managed DB password requires a manual UI step that no tool can perform or verify. Adding it would let the whole rotation run through the mandated infraops lane. Note the related constraint that made GAP-7 bypass infraops for env writes too: `coolify_update_app_env` takes the value as a literal tool argument, so using it for a secret writes that secret into the session transcript — a secret-safe write path (or a "value from file/stdin" affordance) is the general fix. — added 2026-07-29
- [x] (P2) `is_expansion()` (kernel/authority.py) has zero call sites in src/ — verify the CLAUDE.md invariant "authority-expanding standing-context updates require a named human approval" is actually wired to it, or remove the dead function. A guard nobody calls is worse than no guard: it reads as protection. **Scoped into WS-P2.15 (fail-closed lifecycle guards), Wave 1** — added 2026-07-09; DONE 2026-07-12 (WS-P2.15): deleted. The invariant IS enforced, by `classify_context_update()` — but that checks standing-context capability *sets*, NOT envelope budgets or capability levels, so envelope expansion now has no detector. Safe only because the envelope is write-once, which `tests/architecture/test_authority_write_once.py` now enforces — a future budget-raising path trips it and must ship a fail-closed check.
- [x] (P2) Work units created via the direct path (`POST /revisions/{id}/work-units`) are not stamped with `constraints.work_unit_id`, so factory-runner will reject them. **RE-DISPOSITIONED 2026-07-28: documented as non-dispatchable — the stronger fact is that the direct path is unreachable by ANY principal in production.** Both `POST /api/v1/revisions` and `POST /api/v1/revisions/{id}/work-units` call `_require_human` while sitting on the machine-only `orchestrator-api` Traefik router, so a browser session's identity is stripped (401) and a SYSTEM bearer is refused as non-human — no actor can reach either. They are the WS-3.1 manual bootstrap path (defaults `intake_source="manual_ws31"` / `activation_source="legacy_manual"`), superseded by intake → decomposition in WS-3.2; their only callers are the `register-revision`/`register-unit` CLI commands and `scripts/drill_common.sh`, all local-only. Production units are born through intake → decomposition → `/review` approval, which DOES stamp the constraint. Established by the 2026-07-27 production drill run; recorded in ADR-0006's record of the human-gate surfaces. The *service* functions behind these routes (`register_revision`, `register_approved_unit`) remain load-bearing — they are reached constantly via `services/package_intake.py` and `services/decomposition.py` — so this is a route disposition, not dead code. — added 2026-07-09; CLOSED 2026-07-28
- [ ] (P2) Provide a helper that computes `authority.conformance` from real repo state (`security_scan.cli.scan` + `portfolio.compliance.build_rows`, both importable and local-only) so decomposition authors do not hand-type the claim — added 2026-07-09
- [ ] (P3) Tighten the dispatch conformance gate: the `standards_touched ⊆ accepted_standards` branch is a tautology if a producer ever echoes `accepted = touched`. Consider requiring `status == "green"` and treating acceptance as evidence rather than a bypass — added 2026-07-09

- [x] (P1) Attestation audit: enumerate every place the system ATTESTS rather than REFUSES, and make each fail closed. The refusal layers held during the WS-6.4 canary (authority tool scoping denied WebSearch; the change-detection guard refused a hollow PR; decomposition_already_approved refused a silent repair; the kill switch blocked dispatch). The attestation layers did not: `make check` exits 0 having verified nothing, pytest exit 5 is swallowed, finalize records a literal 'passed'. A false attestation is not caught by the gates above it — it is trusted by them. Conformance already has this rule (accepted_standards must never echo standards_touched) precisely because someone saw the tautology coming; generalize it. Evidence: ~/docs/software-delivery-system/2026-07-10-ws64-revision2-envelope-evidence.md — added 2026-07-10 — CLOSED 2026-08-03 by WS-P2.31 (PR #136). The enumeration exists: 24 tabulated acceptance points, 2 observed / 2 attested-bounded / 20 attested-unbounded, in ~/docs/software-delivery-system/2026-08-03-wsp231-build-report.md §1. The system reads external reality in exactly two places (services/github_checks.py, estate_landing.py). NOTE two of the three instances this item named were already fixed before the audit ran: the `need` macro makes make check refuse a missing tool, and the pytest exit-5 swallow is now conditional on the repo genuinely having no test files — both from WS-P2.25's Makefile adoption. The fail-closed half was always per-item and is now individually tracked rather than open-ended: max_llm_calls recording shipped in the same PR, the verifier bypass closed in WS-P2.32, and the residue is c9b1086bd78f (reconciliation cannot detect misreporting), 7874128ae3ac (conformance green short-circuit), eed337085a87 (read-surface gap), 5f8cda64491a (unpinned sibling flag). Also established: the conformance anti-tautology 'precedent' is a docstring, not code — there is ONE exemplar of the observed-not-attested move, not two.
- [ ] (2) Split FACTORY_RUNNER_TOKEN into per-repo worker credentials. One shared credential is currently written into 6 repos' Actions secrets (WS-6.4a, Devon's call for the MVP), so revoking one repo's access means rotating all six. Same agent_id, distinct credential_key_ids. — added 2026-07-10
- [x] (P1) Human-judgment -> passed-adjudication for judgment_required ACs isn't wired to the /review UI. Completion needs a satisfying adjudication for every required AC, but for a judgment_required AC the verifier records none, _authorize_outcome lets only the VERIFIER role record outcome=passed (a human can only 'waive', which needs failed_evidence_id+risk+follow_up), and /review exposes only approval/review/cancel/retry forms — no adjudication form. So a human 'pass' must be recorded out-of-band via the verifier M2M credential. Fix: add a /review adjudication form with a proper human-reviewer authorization model. Surfaced by WS-6.4 canary AC-001. — added 2026-07-10; DONE 2026-07-24 (WS-P2.3: per-AC `/review` adjudication form + route + controlled waiver vocabulary, migration 0017, deployed image 3961e00-wsp23-amd64; program exit #3/#4 met). Closeout: ~/docs/software-delivery-system/2026-07-24-wsp23-closeout-evidence.md
- [ ] (P3) Parameterize _build_intake_payload with a loader function so intake-protocol-fixture also uses it (DRY the third inlined {**loader(...), idempotency_key, expected_version:0} site in src/orchestrator/cli.py). Pre-existing, surfaced by the PR #45 code review. — added 2026-07-11
- [x] (P1) WS-P2.16 — PR-binding chain: enforce unit-envelope capability vocabulary at ingress, make factory-runner POST its PR binding before submit, then add the attempt-scoped EXECUTING→SUBMITTED binding guard; plus a self-discovering cross-boundary vocabulary detector. Deploy order is mandatory (runner before guard) or the factory halts. U2 (factory-runner) is HAND-BUILT — factory-runner has no factory-runner-pilot.yml, so it cannot be dispatched. Wave 2 (WS-P2.5/P2.6) depends on this link. Plan: docs/superpowers/plans/2026-07-12-wsp216-pr-binding-chain.md — added 2026-07-12; DONE 2026-07-23 (built as plain PRs U1+U3+U4+U5 — factory-runner Step 2 was already merged; each independently adversarially reviewed; PRs #62+#66 merged; deployed image 4cfa0c8-wsp216-amd64, migration 0015, pre-flight cleared. Wave 2 UNBLOCKED. Closeout: ~/docs/software-delivery-system/2026-07-23-wsp216-closeout-evidence.md)
- [ ] (P2) Decomposition proposal API: ac_mappings[].ac_id and retained_acs[].ac_id expect the acceptance criterion's database UUID, not the human ac_id string ("AC-001") — decomposition.py:125 builds criteria_by_id keyed on str(criterion.id). The field name and the expected value disagree, and the failure is a generic package_acceptance_criterion_not_found with no hint. Either accept the human ac_id (it is unique per revision — there is already a UniqueConstraint on (work_package_revision_id, ac_id)) or rename the field to ac_uuid — added 2026-07-12
- [x] (P2) Pin WorkUnit.authority as NOT mutation-tracked (a one-line test asserting the column is plain JSONB, not MutableDict.as_mutable(JSONB)). The write-once guard's flag_modified rule works precisely BECAUSE the column is untracked — without it the ORM never notices an in-place dict mutation, so flag_modified's presence is the tell. If anyone wraps the column in MutableDict, alias mutations persist WITHOUT flag_modified and slip the guard. Last hole in tests/architecture/test_authority_write_once.py; found by the WS-P2.15 independent verifier — added 2026-07-12; DONE 2026-07-28 (gap-closure workplan GAP-1): `test_the_authority_column_is_not_mutation_tracked` asserts on a TRANSIENT instance (`as_mutable` coerces at attribute-set time, so no DB/session/flush is involved and no fixture can weaken it), paired with `test_the_pin_would_catch_a_mutation_tracked_column` as the control so the check is not vacuous.
- [x] (P1 — RESOLVED, WS-P2.17 Inc 1 (2026-07-31): `floor_for()` plus HUMAN_FLOOR_TYPES / DETERMINISTIC_PERMITTED_TYPES in services/verifier_evaluators.py give `automated_test` a deterministic-permitted FLOOR — evaluate_criterion resolves it deterministically when readable evidence arrives (the evaluator is chosen by the ARRIVING evidence row's type) and asks a human only when evidence is absent or has no evaluator. `automated_test` is therefore now the correct declaration for an automated criterion; it remains deliberately OUT of DETERMINISTIC_TYPES, because adding it there halts the factory. The item's proposed fix — declare `test` instead — was unfollowable: intent_packages/validate.py's EVIDENCE_TYPES does not permit `test`) Deterministic evaluation of automated ACs. Intent packages declare evidence_type 'automated_test' but the verifier's DETERMINISTIC_TYPES/JUDGMENT_TYPES contain neither it nor any of the other four legal package types, so evaluate_criterion falls through to judgment_required for EVERY automated AC — the root of the 'judgment_required ACs must be passed out-of-band via the verifier M2M credential' gap (a vocabulary gap, not a UI gap). ⚠ **DO NOT "just add automated_test to DETERMINISTIC_TYPES" — that HALTS THE FACTORY, and this item previously said to.** Four adversarial reviews established why: (a) the runner nests exit_code inside verification[] while _status_result reads only top-level fields → failed_closed; (b) the runner writes exactly ONE evidence row per unit (_first_ac_id, factory-runner cli.py:219) while a unit maps to N ACs and the verifier looks evidence up per-AC → ACs #2..N hit `evidence is None` → failed_closed → REVISION_REQUIRED → loop → FAILED; (c) exit_code is a hardcoded 0 (cli.py:486) and _run_command raises on nonzero, so any exit_code-based predicate is constant-true — a fail-open that would auto-pass "the tests pass" on evidence that `uv sync` ran. A correct fix needs: factory-runner writing one evidence row PER MAPPED AC, the verifier keying on the EVIDENCE ROW's evidence_type rather than the criterion's, and a command-aware evaluator. That is a workstream, not a patch. WS-P2.16 ships only the behavior-preserving half (declare the five package types, validate the CRITERION's evidence_type at intake — schemas.py:698, NOT the evidence-row types at :75/:857). Plan: docs/superpowers/plans/2026-07-12-wsp216-pr-binding-chain.md §3.5 — added 2026-07-12; STILL OPEN: WS-P2.16 U4 shipped the safe half (vocabulary declared + intake validation + Assertion D, deployed 2026-07-23); this item is now scoped to the DEFERRED deterministic evaluator only (per-AC runner evidence + evidence-row-type keying + command-aware evaluator)
- [ ] (P2) Evidence-row evidence_type is uncorrelated with the criterion it discharges. services/evidence.py accepts a free evidence_type from the writer (factory-runner hardcodes 'runner.pr.opened'; the verifier writes 'verifier.finding'), but evaluate_criterion keys ONLY on criterion.evidence_type — so an evidence row can carry any type at all and nothing checks it against the criterion it is discharging. A fourth free-string field on the same conceptual vocabulary. This is the blind spot WS-P2.16's vocabulary detector explicitly cannot see, and it is a prerequisite for the deterministic-evaluator workstream above — added 2026-07-12
- [x] (P2) Capture per-attempt actual llm_calls/token consumption so the cost SLO and WS-P2.4 budget enforcement become computable — added 2026-07-23. Plan: docs/superpowers/plans/2026-07-24-cost-actuals-capture.md; DONE 2026-07-25 (WS-P2.4 Inc 1, PRs #69/#20, deployed image bc8eb96-wsp24-amd64). WS-P2.4 Inc 2 (enforcement, PR #70, image d644d8b-wsp24inc2-amd64) closes Wave-1 exit #12. Closeout: ~/docs/software-delivery-system/2026-07-25-wave1-close-wave2-start-closeout-evidence.md
- [ ] (P2) WS-P2.4 follow-ups (deferred, non-blocking): reclaim-path budget halt records reason `lease_expired` not `budget_exceeded` → the `budget_breach` SLO metric undercounts reclaim halts; the reclaim/recover idempotent-replay allowlist omits `budget_exceeded` (a retried reclaim → idempotency_conflict, unit still stays FAILED); `cost_per_unit` metric holds a window SUM not a per-unit rate (rename/clarify); no zero-cost-events coalesce test; SYSTEM-bypass/`role_forbidden` cost-actuals coverage; concurrent-idempotency test uses two-units-same-key (SYSTEM-same-unit is the realistic path); dedup `_UNKNOWN_COST`/`_UNKNOWN_COST_ACTUALS` — added 2026-07-25
- [ ] (P3) WS-P2.5 evidence-pack markdown redaction is two hand-edited helpers, not a structural allowlist — a future markdown section interpolating approver identity/rationale could silently re-leak to a possibly-public PR comment. Make it a structural (denylist-of-fields or allowlist-of-fields) redaction so new sections are safe by construction. Also: envelope rendered as Python repr (not JSON) in the markdown; first-match marker scan could target a human comment quoting the marker; redundant `gh pr view` in the relay — added 2026-07-25
- [ ] (P2) WS-P2.5 Increment 1.5 — post-verification refresh of the PR-comment Evidence Pack. The comment is posted as-of-submit (Option A), so verification/adjudication/deploy outcomes that land after submit are not reflected until a refresh (a late runner step, or an orchestrator→GitHub post capability). Handoff (Inc 2): ~/docs/software-delivery-system/2026-07-25-wsp25-inc2-handoff-prompt.md — added 2026-07-25
- [ ] (P3) SLO report helpers `_intake_to_first_work`, `_queue_age`, `_evidence_completeness` (services/slo_report.py) issue one query per revision/unit (N+1) instead of a single windowed aggregate. Fine at the factory's current volume; fold into aggregate queries if per-window unit counts grow past hundreds. Surfaced by the WS-P2.2 final review — added 2026-07-23
- [ ] (P3) Register the orchestrator prod image build lane in security-standards/governance-map.toml (generates OWNERSHIP.md; not hand-edited from this repo). The .github/workflows/release-image.yml build+push workflow (image-build-automation, PR #76) has no owning lane; governance-map has no lane taxonomy that fits a CI build-and-push workflow (detect/mutate/approve/host), and orchestrator isn't in the map at all yet despite being a BWS consumer. Needs a lane-taxonomy decision (add a 'build' lane) + orchestrator [[repo]] + image-build [[tool]] entries. Proposed TOML is in .superpowers/sdd/task-4-report.md and the closeout ~/docs/software-delivery-system/2026-07-26-wsp26-and-image-build-closeout-evidence.md — added 2026-07-26
- [ ] (P3) .github/workflows/release-image.yml uses actions/checkout@v4 and docker/setup-buildx-action@v3, which GitHub is forcing onto Node 24 (Node 20 deprecated). Cosmetic warning on every run; bump to versions that target Node 24 when convenient. Surfaced by the first real release-image dispatch 2026-07-26. — added 2026-07-26
- [x] (P2) The WS-P2.6 traceability query observation tail is plumbing-complete but empty in prod: the generic Observation table's only producer is POST /api/v1/observations, and no external monitor (watchtower/healthchecks/uptime/github/drift_digest) is wired to post. So 'why is this in production?' currently can't show the continuous external-reality tail (independent health / digest_divergence / split-brain conditions). Wire at least one observation producer so the tail carries data and exit-criterion-#6's observation node is exercised end to end, not just linked. Relates to WS-P2.8 (follow-up scheduling produces observation-typed units). — added 2026-07-26. DECIDED 2026-07-27: first producer is the daily infra-drift digest (`drift-audit.sh` → POST /api/v1/observations, source_system=drift_digest), scheduled as WS-P3.0 right after WS-P2.7 Inc 2, before WS-P2.8. Program home: ~/docs/software-delivery-system/2026-07-27-program-phase3-input-layer-plan.md DONE 2026-07-27 (WS-P3.0): first external producer wired — `drift-audit.sh` posts one observation per audited Coolify instance (`source_system=drift_digest`, `observation_type=drift`, `subject_type=service`) under a dedicated `orchestrator-drift-reporter` credential attributed to the `drift-reconciler` registry actor. Six-run drill evidenced (first post, same-report dedup, second-report append with no conflict, existing outputs unchanged, two negative paths). **Caveat, deliberately not overclaimed:** these rows do NOT appear in the traceability chain — `services/traceability.py` filters the observation hop on `subject_type="work_unit"` — so exit criterion #6's observation node is still not exercised end to end. That needs per-unit observations or a traceability change; tracked separately below. Plan: docs/superpowers/plans/2026-07-27-wsp30-drift-digest-observation-producer.md. Closeout: ~/docs/software-delivery-system/2026-07-27-wsp30-closeout-evidence.md
- [x] (P3) WS-P2.7 Inc-1 (outbound tracker projection) deferred Minors, from the final Opus whole-branch review (non-blocking; fold into WS-P2.7 Inc 2): assert `seen==[]` in `test_write_to_a_transition_path_is_forbidden`; full-`Action`-tuple asserts on the two `projection.py` skip-tests; tests for `TodoistProjector.update_item` url-fallback + non-2xx and for `cli.py`'s two env-token-guard exits; a WORKER-credential case on the `GET /api/v1/tracker-bindings` test (proves auth-only, not just SYSTEM); the create-then-record non-atomicity (a Todoist create that succeeds before the binding write fails leaves a duplicate card next pass — tracker-tidiness only, never canonical; noted in ADR-0003 cost). Closeout: `~/docs/software-delivery-system/2026-07-26-wsp27-inc1-closeout-evidence.md`; Inc-2 handoff: `~/docs/software-delivery-system/2026-07-27-wsp27-inc2-inbound-handoff-prompt.md` — added 2026-07-27; DONE 2026-07-27 in WS-P2.7 Inc 2 (inbound reconciliation, branch `wsp27-inc2-inbound-reconciliation`; all six minors folded in). Plan: `docs/superpowers/plans/2026-07-27-wsp27-inc2-inbound-reconciliation.md`; ADR-0004. DEPLOYED 2026-07-27: image `8da4af3-wsp27inc2-amd64` (digest `sha256:2fc5463…`), migration `0019_wsp27_tracker_recon` applied; running RepoDigest + `/api/v1/reconciliation/tracker-detect` route verified in prod; live `reconcile` pass clean (4 bindings read, 0 conditions — no false-fire). Closeout: `~/docs/software-delivery-system/2026-07-27-wsp27-inc2-closeout-evidence.md`.
- [ ] (P3) WS-P2.7 Inc-2 follow-up (fold into WS-P2.8): make the adapter `reconcile()` read loop per-item fail-open. Today one flaky/rate-limited (429/500) Todoist read raises `RuntimeError` from `TodoistProjector._get` and aborts the whole inbound pass before any report is sent — fail-safe (never corrupts canonical state), but a single bad card blocks reconciliation of all cards, which matters once the pass runs unattended on a scheduler. Fix: wrap `projector.item_completed` per-item in `try/except`, skip-and-count the failure (mirroring the orchestrator's own fail-open-counted posture), still send the partial report. From the WS-P2.7 Inc-2 final Opus whole-branch review. — added 2026-07-27
- [ ] (P3) Reconcile the pre-existing prod model↔DB schema drift. `alembic check` against prod (2026-07-27, at head `0019_wsp27_tracker_recon`) reports a batch of `modify_nullable` (timestamp/event_id columns) and `remove_index` operations across ~10 tables (deployment_observations, dispatch_records, observations, reconciliation_conditions, release_artifact_bindings, unit_pr_binding, …). Long-standing baseline drift from hand-written migrations that don't round-trip autogenerate; NOT introduced by any WS-P2.7 migration and NOT gated by `make check`, so it's latent (the app runs fine). Decide whether to (a) generate a reconciling migration to bring the DB to model parity, or (b) adjust the models to match the DB, then consider adding `alembic check` to CI to prevent recurrence. Surfaced during the WS-P2.7 Inc-2 deploy verification. — added 2026-07-27
- [x] (P2) Run the five recovery drills against production per ADR-0005 (docs/decisions/0005-production-drill-disposition.md, disposition A recommended) in a dedicated authorized session, then update program exit criteria #5/#7 to MET with the retained evidence and close remediation 0.3 in docs/superpowers/plans/2026-07-12-remediation-order.md. ADR accepted 2026-07-27 (disposition A); run after PR #82 merges (held until WS-P2.7 Inc-2 completes), prerequisites 2–5 of the ADR still to be met first — added 2026-07-27 — DONE 2026-07-27: 5/5 pass against sds.alobar.net, evidence + HUMAN closeout in ~/docs/software-delivery-system/2026-07-27-production-recovery-drill-run.md; #5/#7 MET; remediation 0.3 closed
- [x] (P2) Decide the human-credential trilemma before scoping WS-P2.9 (docs/superpowers/plans/2026-07-12-factory-next-steps.md): a HUMAN credential path (device/OIDC via Alobar ID) vs POST routes in /review vs an explicit permanently-browser-only ruling for the two fetch()-only gates. Prerequisite for the factory CLI front door and for every Phase-3 automated proposal lane — added 2026-07-27; DONE 2026-07-28 (Devon): option (c) with (b) as its implementation — human gates are **permanently browser-only** and **no standing HUMAN credential will ever exist**. Recorded as ADR-0006 (`docs/decisions/0006-human-gates-browser-only.md`), which also closes remediation 3.1/3.2. Implemented in the same PR: the `/review` package-intake form (`GET /review/intakes/new` → `POST /review/intakes`) retires the last devtools `fetch()` gate, reusing the API route's `PackageIntakeRegistration` validation and `register_package_intake` service so the browser path is a new way in and not a new set of rules; the two CLI commands that can never satisfy `_require_human` against production (`intake-package`, `record-approval`) are annotated local-development-only. **Consequence for WS-P2.9: the CLI wraps non-human surfaces and deep-links/clipboards the `/review` gates — it must never grow a flag that impersonates a human.**
- [ ] (P2) Exit criterion #6's observation node is still not exercised end to end: `services/traceability.py` filters the observation hop on `subject_type="work_unit"` and the unit id, so the WS-P3.0 drift observations (`subject_type=service`) never appear in a traceability chain. Decide between (a) teaching the traceability query to include environment- or service-scoped observations for a chain's deployment environment, or (b) accepting that only per-unit observations count and finding a producer that emits them. Blocks any claim that the observation tail is answerable from the traceability query. — added 2026-07-27
- [ ] (P3) Per-application drift observations and observation supersession. WS-P3.0 posts an append-shaped per-instance digest because day-over-day changes to the same finding would need supersession, which does not exist (`observation_conflict` rejects the same source reference with different facts). Once WS-P2.8 exists and supersession is specified, revisit emitting one observation per drifting application (`subject_reference` = the app), which is the shape that can become proposed work. — added 2026-07-27
- [ ] (P2) The WS-P3.0 producer hand-mirrors the orchestrator's six closed observation vocabularies (`OBSERVATION_SOURCE_SYSTEMS`, `OBSERVATION_TRUST_CLASSIFICATIONS`, `OBSERVATION_SUBJECT_TYPES`, `OBSERVATION_TYPES`, `OBSERVATION_STATUSES`, `OBSERVATION_SEVERITIES`), the `SECRET_KEY_PARTS` denylist, and four numeric bounds (facts byte size, key length, string length, list length) into TypeScript in `infraops-mcp-server`, with no test spanning the boundary. `tests/architecture/test_cross_boundary_vocabulary.py` only walks `src/orchestrator/`, and the `OBSERVATION_*` tuples are DB-`CheckConstraint`-pinned, which that guard treats as a structural exclusion — so the copy is invisible to it by construction. A future edit to either side leaves both suites green and silently breaks the nightly post (fail-open, so it degrades to a WARN in a log nobody reads). Precedent for the fix is the factory-runner envelope contract: a byte-identical shared fixture plus a shared `CONTRACT_SHA256` asserted in both repos. — added 2026-07-28
- [ ] (P2) Evidence Pack shows the unit's LIVE authority envelope, not the `enforcement_snapshot` the revision was admitted under. This is an accuracy defect distinct from the WS-P2.5 redaction item above: the pack is meant to answer "what was this change authorised to do", and a reader gets today's envelope rather than the one the human approved. Render the `enforcement_snapshot` (and label which is which), or state plainly in the pack that the envelope shown is current-state. Surfaced by the 2026-07-28 Wave-1/2 reconciliation sweep — added 2026-07-28
- [ ] (P3) Evidence Pack omits the standards and skill versions in force at the time of the work — fields the release definition names, and the ones that let a reader reconstruct which rules applied. Add them from the revision's recorded enforcement state rather than from the current repo. Surfaced by the 2026-07-28 Wave-1/2 reconciliation sweep — added 2026-07-28
- [ ] (P3) The Evidence Pack is recomputed on every read, so a pack fetched after later adjudications/waivers no longer shows what was true at release time, and two readers can see different packs for the same release. Consider an immutable snapshot written at a defined moment (submit, or completion) with the live view kept as a separate, clearly-labelled projection. Interacts with WS-P2.5 Increment 1.5 (post-verification refresh) — decide the two together, since one wants freshness and the other wants fixity. Surfaced by the 2026-07-28 Wave-1/2 reconciliation sweep — added 2026-07-28
- [ ] (P3) The `/review` per-AC adjudication form offers `passed` / `not_applicable` / `waived` but **not `failed`** — so a human who judges a criterion genuinely unmet has no way to record that verdict through the UI; the options are to pass it, call it inapplicable, or waive it (which demands a `failed_evidence_id`, a risk class and a follow-up). Add `failed` to the form's vocabulary, or document why a human-recorded failure must instead go back through revision. Surfaced by the 2026-07-28 Wave-1/2 reconciliation sweep — added 2026-07-28
- [ ] (P3) Todoist reopen/delete divergence in the tracker projection: a card completed in Todoist and then reopened, or deleted outright, is not distinguished from one that was never touched, so the inbound reconciliation pass can read a stale or absent card as agreement. Define the intended semantics for both events and make the detector fail-open-counted rather than silent. Surfaced by the 2026-07-28 Wave-1/2 reconciliation sweep — added 2026-07-28
- [x] (P1 — RESOLVED, 2026-07-29 (GAP-4): the decision was taken and the dispatch ran end to end on attempt 3. Attempts 1-2 failed because closing the bounded window restarts the orchestrator and the restart met the runner's finalize-run; attempt 3 held a ~13.5 min window and succeeded. See the CLAUDE.md invariants on window-close timing and on reused runner_attempt ordinals) **Decide whether to run the first real dispatch** (gap-closure workplan GAP-4) — a deliberate decision named as pending in three closeouts and never taken. One small dependency-update unit through the full dispatched lane with `ORCHESTRATOR_DISPATCH_ENABLED` on for it, then off again. It is the only thing that exercises the evidence-pack PR comment, cost-actuals capture, the budget cap and the PR-binding guard under real runner traffic, and it closes Wave-2 exit clause 1 (no PR in the org has ever carried an Evidence Pack). **Prerequisite: the factory-runner Phase-1 pair (GAP-2)** — do not send real traffic through a lane with a live lease-token-leak defect. Known constraint to plan around: the evidence-pack comment is as-of-submit, so the first real pack shows pre-verification state. Workplan: `~/docs/software-delivery-system/2026-07-28-wave12-gap-closure-workplan.md` — added 2026-07-28

- [x] (P2) Nothing surfaces FAILED work units awaiting operator disposition. FAILED is not terminal — (FAILED, READY) and (FAILED, CANCELLED) are both legal edges — so a FAILED unit is an unsettled resting state whose disposition (retry or retire) nobody is prompted to take. dead_letter reports FAILED units but reporting is not resolution, and WS-P2.8's due predicate makes the cost concrete: a revision with one lingering FAILED unit is permanently unschedulable for follow-up, and the only signal is that pass's unsettled_failed_unit skip reason. This is a second silent-stall class alongside the stalled-approval one WS-P2.15 closed. Surfaced by the WS-P2.8 design PO review 2026-07-28. — added 2026-07-28; DONE 2026-07-31 (WS-P2.17 Inc 5): a FAILED unit is now a pending-decision kind in the `/review` queue, naming retry-or-cancel, and reading differently depending on whether attempts remain. Control run proved the gap was real — with a FAILED unit in the database the queue previously rendered "Nothing is waiting on you." Merge `76b46c8`.
- [ ] (P2) No reserved-AC-id guard: a package that declares an acceptance criterion literally named 'follow-up-review' (or any 'post-deploy-*' id) collides with the orchestrator's generated criteria and makes the affected work unit permanently undischargeable. load_required_criteria returns the GENERATED criterion (evidence_type observation, so /review renders a human outcome), while _criterion_evidence_type finds the PERSISTED declared row first and returns its type — if that type is deterministic, _authorize_outcome refuses the human 'passed' forever. package_intake validates AC uniqueness and the evidence-type vocabulary but reserves no ids. Affects both generated-AC families; WS-P2.8 added a sixth reserved id without closing it. Surfaced by the WS-P2.8 Task 5 review 2026-07-28. — added 2026-07-28
- [ ] (P2) drill-6 — dispatched lane: quarterly recovery drill exercising a real factory-runner dispatch end to end (drill-labeled unit, bounded window, terminal-before-close protocol). GAP-4 needed three attempts and surfaced two real defects; this lane's failure modes are otherwise un-drilled. See docs/software-delivery-system/2026-07-29-gap4-closeout-evidence.md — added 2026-07-29
- [x] (P1 — RESOLVED in two halves, WS-P2.17 Inc 2 + Inc 4 (2026-07-31). The FORM half: `human_may_adjudicate()` is now the single predicate behind evaluation, authorization (evidence._authorize_outcome) and the /review per-criterion flag, so judgment ACs are adjudicable from the GUI and no longer need the verifier M2M credential out-of-band. The EVIDENCE half, which was this item's core complaint: unit.html now renders each criterion's `stable_ref` and its full `payload` as pretty-printed JSON, not a one-line summary) The /review UI cannot present evidence for judgment, so judgment_required ACs are unjudgeable from the GUI. unit.html renders evidence as a one-line summary (ac_id: evidence_type; current/superseded) and never renders the payload; evidence_pack.html renders `stable_ref or payload`, so setting stable_ref suppresses the payload entirely, and when payload does render it is a raw dict repr in a table cell. Every AC in the non-software-operational profile is judgment_required, so this blocks the whole class. Found 2026-07-30 driving WS-P2.13 first real unit: Devon opened the adjudication form and had nothing to judge. WS-P2.3 shipped the adjudication form without the evidence rendering behind it. — added 2026-07-30
- [x] (P1 — RESOLVED, WS-P2.17 Inc 3 (2026-07-31): `record_adjudications()` (plural) makes a multi-criterion submission atomic, so a refusal on the third criterion no longer leaves the first two committed, and the outcome `<select>` now defaults to a selected '— No decision —' instead of `passed`. NOTE the item's stated MECHANISM was wrong and should not be re-derived from it: adjudications never write work_units.version (its three writers are all state transitions), so the per-criterion forms did not staleness-break each other. The real defects were the missing atomicity and the select default) The /review per-AC adjudication form is unreliable in use: recording two criteria in sequence produced an outcome Devon did not intend (AC-002 landed not_applicable when passed was intended). Each AC is its own form carrying a hidden expected_version stamped at page render, so the first submission bumps the version and the second form on the still-rendered page is stale. Observed 2026-07-30 WS-P2.13. Pairs with the evidence-rendering P1 8fceb3ac16bf; both are /review GUI work. — added 2026-07-30
- [ ] (P3) BWS secret CREATION has no local path: every machine-account token on this Mac (SDS and the broad account) gets 404 Resource not found on bws secret create, against both the SDS Operator and Ops/Platform projects — that is how Bitwarden refuses a write for a read-only account. The note claiming the broad account has read+write on Ops/Platform is stale. Also bws secret create takes the value as a POSITIONAL argument (no stdin, no file), so any automated creation puts the secret in argv. Probed 2026-07-30 while attempting to register the orchestrator-operator token; consequence is that registering it needs the Bitwarden console or a write-capable operator-onboarding tool. Context for backlog item 2fed026f5638. — added 2026-07-30
- [x] (P2) The orchestrator-operator M2M token (agent_id claude-code-interactive, key-id orchestrator-operator) lives only in the macOS Keychain (Claude / ORCHESTRATOR_OPERATOR_TOKEN), not in BWS. Every other orchestrator M2M bearer is BWS-resolved by stable UUID, which is the portfolio secret-handling standard. Minted 2026-07-30 during WS-P2.13 because the sds-operator account is read-only and cannot create secrets, and minting a write path mid-credential-rotation would have stacked variables. Register it in the SDS Operator BWS project and add it to .bws-secrets.toml. — added 2026-07-30 DONE 2026-07-30: registered as BWS `orchestrator-operator-bearer` `bd71bed1-4aac-4af8-9094-b4970180bc59` in the SDS Operator project, added to `.bws-secrets.toml` so the drift scanner watches it, and recorded in the CLAUDE.md credential-fetch invariant. Full chain proven live: Keychain(BWS_ACCESS_TOKEN_SDS) -> `bws secret get` -> Bearer -> `GET /api/v1/status-ledger` 200, with two controls (wrong token same key-id 401; right token wrong key-id 401). Stored value hashes to 18193828df5b, matching what Coolify holds. Note: no local BWS token can CREATE a secret (all get 404), so the console was the only write path — see the P3 item below.
- [ ] (P2) Timing-approval gate: a 'everything is set, is it ok to run this now — it will take X and may impact your machine' form Devon clicks to start a window, plus an earned skip list (work he approved the timing of once can be marked as not needing timing approval in future), plus routing a set of work through change-manager's change window. PARKED 2026-07-31 pending Wave 3. Key finding from the change-manager read that day: there is NO change window in change-manager — WindowRun is a post-hoc log of a batch run (no starts_at, no schedule, no scope, no approver); the actual window is a LaunchAgent cron at 04:00 on the Mac mini running change-window.sh capped at MAX_CHANGES_PER_WINDOW=5, with the policy living in a plist and an env var in infraops-mcp-server, unreadable by change-manager. change-manager also has no scheduler, no background worker, no outbound HTTP client, no notification (all emails come from infraops-mcp-server), and no knowledge that the orchestrator exists; infra_lane_links is a one-sided memo the orchestrator writes to itself. Consequence: 'connect SDS to change-manager' rests on a premise that does not hold, but the work is SMALLER than it looks — infra-lane work is ALREADY governed by the 04:00 executor, it is merely unrepresented, so it needs representation not a new window; only SDS work touching Devon's machine or external systems needs the form; repo-only work skips immediately. Devon leans connect-not-absorb. Notes: ~/docs/software-delivery-system/2026-07-31-simplification-review-notes.md (R13) — added 2026-07-31
- [x] (P1) The named-check ASSERTION mechanism cannot fail unless the operator chooses to make it fail — so the one deterministic AC path rests on operator honesty, not on observation. _named_check_result compares payload['assertions'][i]['expected'] against ['observed'] and fails closed on mismatch — but BOTH sides are supplied by the caller on the same command line. An assertion is therefore 'the two values I typed match each other'. Combined with the already-known fact that nothing queries GitHub's check-runs API (check_name, conclusion, run_id, run_url are all typed), the automated_check path is: canonical BINDING is machine-verified (dispatch record, PR binding, armed head, repository all checked against stored state — this part is genuine), while the CONCLUSION is entirely attested. Demonstrated live 2026-07-31 in the WS-P2.21 certification: AC-001 resolved passed with decided_by orchestrator-verifier, on assertions the operator typed both halves of. This is the standing attestation P1 at its most load-bearing, because it is the mechanism the whole 'deterministic evaluation' claim rests on. Fix direction: the verifier fetches the check run from GitHub and derives conclusion/assertions itself, which also requires disambiguating the two identically-named Quality runs a PR head carries (push + pull_request) — added 2026-07-31 — CLOSED 2026-08-02 by WS-P2.20 (PR #133, 8e13258): the caller now supplies only check_name and expected_conclusion; the orchestrator reads the job's conclusion from GitHub at ingestion and records it under `observation`, and `assertions` is gone. Duplicate same-named runs resolve only on unanimity. NOTE the App has no `checks` permission (403 on the Checks API, measured), so the read is of GitHub Actions workflow JOBS. Report: ~/docs/software-delivery-system/2026-08-02-wsp220-build-report.md
- [ ] (P2) scripts/*.py sits OUTSIDE the repo-wide outbound-egress scan, so the push-only invariant is unenforced there. test_wsp21_invariant_scan.py walks a root that excludes scripts/, meaning a script may import an HTTP client and make outbound calls without appearing in OUTBOUND_ALLOWLIST or being noticed. This matters because the scan IS the enforcement of the architectural rule that the orchestrator never pulls external reality — a gap in its walk is a gap in the invariant, not merely in a test. Note this is unlikely to be a one-line fix: scripts/ contains attest_exit_criteria.py (which probes production routes) and the drill scripts, so extending the walk will surface several files that need allowlist entries with real reasons — which is the point, but it is a small piece of work with judgment in it rather than a config change. Found 2026-08-01 by the WS-P2.23 build session and named rather than silently fixed — added 2026-08-01
- [x] (P1) budgets.max_llm_calls is a term of the authority envelope Devon APPROVES that has no runtime effect and leaves no trace when exceeded. is_over_budget (services/budget.py) compares cumulative recorded llm_calls to the declared ceiling, and is consulted in exactly two places, BOTH at claim time (claims.py:74 and :583). So an attempt that blows its ceiling mid-run is never checked again if the unit completes: no budget_exceeded event (that only emits when a CLAIM is refused), no SLO budget_breach entry (it counts those same refusal events), nothing in the evidence pack. Demonstrated live 2026-07-31 in the WS-P2.21 certification: envelope declared max_llm_calls 4, the attempt recorded 16 (25 turns, $0.3849), completed normally, and the system's own records show no overrun. The ceiling is inside the authority fingerprint, so the human approval attests to a cap that is not enforced and whose breach is invisible. Note max_attempts, the envelope's OTHER budget field, IS enforced — so one of the two is decoration. FIX DIRECTION (cheapest first): when cost-actuals arrive and cumulative exceeds the ceiling, emit a breach event. The orchestrator is push-only and cannot interrupt a running runner, so prevention is not available to it — but RECORDING is, and it turns a silent overrun into a fact that reaches the SLO report and the evidence pack, reusing a metric that already exists. Real mid-run enforcement would mean the runner reading its ceiling from the brief and stopping itself, which is the runner attesting to its own compliance and is therefore weaker than it sounds — a later pass. Same defect family as the arbitrary 15-minute lease and the missing stall control: a declared bound that does not bind, discoverable only by inspection — added 2026-08-01 — CLOSED 2026-08-03 by WS-P2.31 (PR #136), along the item's own cheapest-first fix direction: a breach is now RECORDED where the SLO report and the evidence pack see it, never prevented — the orchestrator is push-only and cannot interrupt a running runner, and making the runner stop itself would be the runner attesting to its own compliance. Controls ran both directions (emitter off -> 6 assert-a-breach red, 7 controls green; emitter forced on -> exactly the 4 over-emission constraints red). /code-review caught a real bug in the first predicate: it keyed on the CROSSING, and max_llm_calls: 0 is a ceiling already met before any spend, so no crossing ever occurs for the unit that most obviously overran; now keyed on at-or-over plus no-breach-on-file. CORRECTION to this item's own text: 'max_attempts, the envelope's OTHER budget field, IS enforced' is WRONG. budgets.max_attempts has no enforcement reader either; the enforced cap is unit.max_attempts, a different value on a different column from a different API field, never compared. Both envelope budget fields are decoration, and that one is worse because the name collision hides it.
- [ ] (P3) Enforce budgets.max_llm_calls during a run, not only at re-claim: is_over_budget is consulted solely in _readiness_eligibility_error (reclaim/requeue), so a unit can overrun its declared budget within one attempt. GAP-4's envelope declared max_llm_calls 4 and attempt 3 recorded 15 and completed normally. The field reads as a spend cap but is not one. — added 2026-08-01
- [ ] (P3) QUESTION, not pending work: what attests an orchestrator self-build that has no work unit? WS-P2.18 closed at Inc 8 with policy 4 half-delivered — its 'when' shipped (live_estate's change window governs the orchestrator restarting itself), its 'to what' did not. Inc 7 established the blocker is NOT a missing model: release_artifact_bindings already describes a deployable in nineteen columns (registry, digest, tag, source and merge commit, workflow run, provenance, SBOM). The blocker is its ANCHOR — work_unit_not_completed means a self-build can never have a row, and a self-update has no package, no revision and no enforcement snapshot, so there is also no declared reach to key policy on (a consumer constructing ('live_estate',) and handing it in is the one thing R8 exists to prevent). Decide the attestation before writing code. AND DECIDE WHETHER IT IS WORTH DOING AT ALL: it automates a trigger that fires a few times a week and removes a human digest verification that earned its place on 2026-08-02, when HQ's digest-pin recommendation was wrong and the manual check is what catches that class. R11 says deploy is not a human gate — that means it needs no approval, not that it must be automated. Also blocked-adjacent: no orchestrator M2M credential is available to this repo's CI (only the factory-runner pair, whose agent_id must not be borrowed), so any ingress needs a new registry actor and an image rebuild — added 2026-08-02
- [x] (P1) reach is determined by inspecting the DEPLOY TARGET's config, which structurally cannot see a trigger that lives in the SOURCE REPO and points at the target — so a repo that redeploys itself on merge reads as inert. Found 2026-08-02 driving the first pattern-recognised unit end to end. R13 concluded 'merging a PR deploys nothing' for change-manager from Coolify's own settings (source_id null, source_type null, manual_webhook_secret_github null — i.e. Coolify will not PULL on push), and that verification is sound but answers the wrong direction: change-manager's OWN .github/workflows/deploy.yml runs on push to main and its build-and-deploy job ends with a step named 'Trigger Coolify redeploy' that curls a COOLIFY_DEPLOY_WEBHOOK secret. Confirmed empirically — merging PR #40 fired a deploy run two seconds later. CONSEQUENCE: the change-manager-fastapi-bump package declared reach [source_repository] and its scope.excluded asserted 'a landed pull request is inert until something separately triggers a deployment; this package does not do that and must not', which is FALSE for that repository. The unit's own work (open a PR) was genuinely inert so the unit was not misrouted, but the merge is live_estate work and it happened outside the 02:00-06:00 window live_estate declares. Harmless here (Devon merged knowingly, construction-mode estate, change-manager is behind Alobar ID) but the determination method is wrong for every repo that self-deploys. FIX DIRECTION: reach determination must read the SOURCE repo's workflows for outbound deploy triggers, not only the deploy platform's inbound config; candidate signal is any workflow on push-to-default-branch that calls a deploy webhook or platform API. Note this also means a package author cannot honestly declare reach without inspecting the target repo's CI DEVON'S FIX DIRECTION (2026-08-02, supersedes the CI-scraping direction below as the primary approach): put the application knowledge in App Brain — how each app is deployed, and whether a merge to its default branch reaches anything live — and have the SDS ASK App Brain rather than infer. That removes the burden this item otherwise puts on package authors (a human cannot honestly declare reach if doing so requires reading the target repo's CI), and it puts the fact where it is already supposed to live: App Brain is the read-only source for what an app is and how it is deployed. Scraping the target repo's workflows stays useful as a CROSS-CHECK that App Brain's answer is still true, not as the source of truth. — added 2026-08-02 — CLOSED 2026-08-03: superseded by Devon's own fix direction, which shipped. WS-P2.29 made the landing fact a structured served fact determined across all three trigger mechanisms (workflow step, repository webhook, platform git integration — no single source shows all three), and WS-P2.28 made the orchestrator ASK App Brain rather than infer. The CI-scraping direction recorded above was never the primary approach and remains available only as a cross-check.
- [ ] (P2) Deterministic deploy tool — one command that takes a revision and returns a verified deployment or a refusal. DEFERRED DELIBERATELY by Devon 2026-08-02: the deploy process may still change while it is being built out, and this belongs with the broader wave to make the rest of the system use deterministic tools rather than inference. Do NOT build it against today's procedure; hold it to the PROPERTIES in the spec, not the step list. Spec: docs/superpowers/specs/2026-08-02-deterministic-deploy-and-the-inference-inventory.md. Key point that kept this stuck: it was mis-scoped under policy 4 / self-update, which is blocked on an authority question — but an operator running a tool has no attestation problem, no work unit and no control-plane egress, so this is UNBLOCKED and must not be sequenced behind policy 4. Six measured inference points (label choice, migration check ordering, --ref vs -f ref=, the unrecorded rollback target, finding the pushed digest, the container-to-image-to-RepoDigest recipe that CLAUDE.md documented wrongly until 2026-08-02). Acceptance test for the whole thing: runnable by someone who has read none of CLAUDE.md — added 2026-08-02
- [x] (P1) Onboard intent-packages and project-standards into App Brain. WS-P2.29 made 'does merging to this repo's default branch change anything already running?' a structured served fact on the app record, determined for all 25 registered apps — but 2 of the 4 repositories the factory can currently target are NOT App Brain apps at all, so the route answers unknown/no_app_record for them. That is not a gap in WS-P2.29's backfill; it is a gap in the app registry. Consequence, and it is the reason this is P1 rather than housekeeping: WS-P2.28's reach check must fail closed on unknown (an app nobody assessed must not be able to claim a merge is inert), URGENCY CORRECTED 2026-08-02 by WS-P2.28, which measured it: exactly ONE of 25 authored packages declares reach at all, and it targets change-manager. Nothing targeting these two repos declares reach, so nothing is refused on their account today and a release need NOT be blocked on the onboarding. It becomes binding the first time a package targeting either repo declares reach — which is also the first moment it would be silently wrong without this. Do it before that, not before the release. Onboarding is the portfolio/app-brain onboard flow, not a code change. Do it BEFORE WS-P2.28 binds, or accept that two repos are unusable until it happens — added 2026-08-02 — CLOSED 2026-08-03: done by WS-P2.30's repositories entity. Verified against production this date: AlobarQuest/intent-packages and AlobarQuest/project-standards both answer landing=inert (they answered unknown/no_app_record when this item was written). All four factory-targetable repositories now resolve: change-manager redeploys, the other three inert.
- [x] (P1) A VERIFIER credential can drive a unit to COMPLETED with zero evidence, bypassing everything WS-P2.20 built. Two independent halves, both verified 2026-08-03 (WS-P2.31, confirmed again by HQ): (1) _completion_satisfied (services/lifecycle.py:473) takes (required_ac_ids, adjudications, occurred_at) and no evidence parameter — it structurally cannot consult evidence, so completion is decided on adjudication rows alone; (2) _authorize_outcome's VERIFIER branch (services/evidence.py:966) is 'allowed = outcome in NON_WAIVER_OUTCOMES' with no evidence requirement, and _validate_adjudication_fields requires evidence only for 'waived' (failed_evidence_id) — 'passed' needs a rationale string, and evidence_id is validated only when non-null. So POSTing outcome=passed with prose on each required AC completes the unit. The orchestrator-verifier credential (BWS 660d5846-abcb-4751-be86-b483012899eb) is standing in production. This is the sharpest instance of the attestation class: the named-check observer added in WS-P2.20 reads GitHub precisely so the conclusion is not taken on the caller's word, and this path never consults it. FIX DIRECTION: completion must require, per required AC, an adjudication whose outcome is backed by an evidence row the system observed — i.e. give _completion_satisfied access to evidence and make 'passed' from a VERIFIER require evidence_id, keeping the human-judgment path (human_may_adjudicate) as the deliberate exception. Check what that refuses before shipping: measure the existing population first, the way WS-P2.31 measured the conformance echo at zero. — added 2026-08-03 — CLOSED 2026-08-03 by WS-P2.32 (52d7d7e, PR #137): a verifier adjudication may only arise from verify_work_unit; a direct POST is refused as a named verifier_evaluation_required. Shape (c) was shipped, not (a): a non-null evidence_id proves a row exists, not that it supports the outcome, and _validate_evidence_reference accepts the worker's own artifact — strengthening (a) toward derivation collapses into (c) and additionally breaks the verifier's own failed path. Measurement that decided it: 36 of 59 verifier adjudications came through the bypass across 12 units, 17 on ac_ids with no evidence row, three completed production units holding zero evidence; the practice stopped 2026-07-27 when WS-P2.17 Inc 2 gave the human that case. The 18 historical unevidenced completions are left as they are (terminal; back-dating a judgment is the ADR-0014 mistake). NOTE the SECOND half of this item is NOT closed and is tracked separately.
- [ ] (P2) The conformance kit's repo.protection check emits an UNFOLLOWABLE remediation on private repos, which makes exit criterion #2 (>=5 repos onboarded) unsatisfiable as written. Measured 2026-08-03: 6 of 8 candidate repos (change-manager, brain, orchestrator, intent-packages, project-standards, security-standards) are PRIVATE on a free GitHub plan, where GET/PUT repos/{r}/branches/main/protection returns HTTP 403 'Upgrade to GitHub Pro or make this repository public to enable this feature'. The kit reports these as repo.protection=violation and prints the fix 'gh api -X PUT .../protection -f required_pull_request_reviews...', a command that cannot succeed. Only 2 repos are public: infraops-mcp-server (protection already set, which is why the kit passes it) and factory-runner (public since WS-6.4 so the reusable workflow install can authenticate; protection available and NOT set - the one genuinely actionable case). FIX DIRECTION: the check must distinguish UNAVAILABLE from VIOLATION. A 403 with that message means the capability does not exist on this plan, which is a procurement/visibility decision, not a repo defect - report it as unavailable/not-applicable with the decision named, the way the reach check distinguishes no_app_record from not_assessed. Until then criterion #2 cannot be met by any amount of build work. NOTE the same 403 also broke HQ's first probe of this on 2026-08-03: the error body contains neither 'Branch not protected' nor 'Not Found', so a naive branch fell through to an else and reported the six repos as PROTECTED - the exact inverse of the truth. Any probe of branch protection must handle the 403 case explicitly. — added 2026-08-03
- [x] (P1) factory-runner requires constraints.mutation_commands whenever command.run is allowed — UNCONDITIONALLY — while the orchestrator requires it only for change_class 'dependency-update'. The two sides disagree, so the orchestrator ADMITS an envelope the runner REFUSES, and the disagreement is invisible until a real dispatch. Measured 2026-08-03 by the WS maintenance-remediation pilot: unit c609dac5-66e0-5b3f-8545-b3b3d128c712 passed every orchestrator admission term (readiness ready, authority approved, change class allowed, target allowed, reach admitted) and the run died in 14 seconds with 'AuthorityError: constraints.mutation_commands must be a non-empty list of non-empty strings' (intent-packages Actions run 30830197885). Orchestrator side: kernel/runner_authority.py::dependency_update_authority_violation opens with 'if envelope.change_class != "dependency-update": return None'. Runner side: factory_runner/authority.py::_validate_commands requires it under 'if _allowed(envelope, "command.run")' with no change-class condition at all. THE DEEPER PROBLEM, which is the real finding: the runner's model assumes the MUTATION IS A COMMAND. That fits dependency-update (uv add) and does not fit an edit-shaped work unit, where the diff is produced by the coding agent and no command mutates anything — so there is no honest value to put in mutation_commands. Declaring a fig-leaf entry (e.g. 'uv sync', which only touches .venv) would make the envelope lie about what mutates, and the envelope is what a human's authority approval attests. OPTIONS, none free: (a) teach factory-runner that an edit-shaped unit needs no mutation command, keyed on something honest rather than on change_class; (b) accept that only command-shaped work can run through the factory and say so; (c) require every unit to express its mutation as a command, which most remediation cannot. This blocks Wave-3 exit criterion #1 for every non-dependency-update software profile, not just this package. Note the envelope+brief are pinned cross-repo contracts but NEITHER pins this rule, which is why a byte-identical fixture test passes while the two sides disagree. — added 2026-08-03; DONE 2026-08-03 (WS-P2.33): option (a), keyed on change_class because the frozen pilot envelope carries no other discriminator — one shared predicate in both repos (factory-runner #38, orchestrator #138), pinned by a second byte-identical golden fixture (runner_authority_envelope_edit.json, CONTRACT_SHA256_EDIT) with fires-negatives on both sides, each demonstrated to red under a one-sided loosening. Proven end-to-end in production: package revision 4 unit 327920cd completed via PR intent-packages#55 (+1/-1 caller-pin diff, named check observed green). Build report: ~/docs/software-delivery-system/2026-08-03-wsp233-build-report.md
- [x] (P1) Envelope ingress admits shapes the runner refuses (WS-P2.33 review, pre-existing): capability LEVELS unvalidated (validate_unit_capabilities checks names only — 'requires_approval', the package-authority vocabulary, clears admission and dies at the runner as an unsupported level); orchestrator-only capability names (operational_action, post_deploy_verification) are fatal to the runner even at 'prohibited'; any extra top-level envelope key is fatal (runner model is extra-forbid; surfaces as ValidationError, not a named AuthorityError). Same 14-second-death signature as the pilot. Cheapest closure: envelope schema validation at the decomposition gate (top-level keys within KNOWN_FIELDS, levels within {allowed, prohibited}) pinned by set-equality against the runner's model; measure the production population before tightening. — added 2026-08-03 DONE 2026-08-04 (WS-P2.34): levels validated at both ingress paths (unknown_capability_level); extra/malformed top-level fields refused at both via runner_envelope_field_violation, keyed on the unknown_fields record normalize_authority already computes (nothing added to KNOWN_FIELDS — that would rewrite every fingerprint in the ledger); orchestrator-only capability NAMES refused at DISPATCH ADMISSION as capability_outside_runner_vocabulary, not at ingress, because non-software operational units legitimately declare them and never reach a runner. Level AND field vocabularies pinned by a byte-identical tests/fixtures/runner_envelope_contract.json in both repos, from which factory-runner derives its field set out of pydantic model_fields (the golden envelopes provably cannot carry it — every capability in both is 'allowed'); each guard and the pin shown reddening its test under a one-sided edit. Production swept first, all 49 units: 0 bad levels, 0 authored envelopes with unknown fields (2 generated release-verification units carry historical ones, both terminal, both minted outside these gates — the handoff predicted zero), 8 with non-runner capability names, every one terminal. NOT DEPLOYED: enforcement arrives with the next release. Build report: ~/docs/software-delivery-system/2026-08-04-wsp234-build-report.md
- [ ] (P2) _is_skipped_reason (services/dispatch.py) classifies the four WS-P2.33 authority_* command-constraint codes as 'skipped' — the disposition whose own inline comment justifies only dispatch_disabled and OUTSIDE_CHANGE_WINDOW, on the grounds that 'nothing is wrong with the unit and nobody has to act'. That justification is false for an envelope that fails the command predicate: the unit needs a new package revision and fresh human approvals, which is the most expensive remediation in the system. CONFIRMED BY WS-P2.34, which is what raises this above an observation: that session classified its own analogous new reason (capability_outside_runner_vocabulary) as BLOCKED, reasoning that a READY unit with an unparseable envelope needs a person — and a reviewer noted the reasoning applies verbatim to the four it left alone. So shipped code now disposes of two identical situations two different ways. Note the change-window justification is specifically about RECURRENCE (nightly noise in operator surfaces), and dispatch is explicit-only with no sweeper, so an authority violation cannot recur on its own — the argument that warrants 'skipped' for the window does not reach these four. Bounded, not silent: the dispatching caller still sees status/reason_code in the HTTP response; what is lost is the aggregate needs-attention surfaces. Fix: move the four to blocked, and close the second-copy problem in the same change (the set is a hand-maintained duplicate of kernel/runner_authority.py's codes with no sync test — export from the kernel and register per the cross-boundary vocabulary guard). — added 2026-08-04
- [ ] (P2) _is_skipped_reason (services/dispatch.py) classifies all four authority_* violation codes as 'skipped' — operator-invisible — under a comment justifying only dispatch_disabled and the change window; WS-P2.33 widened authority_allowed_commands_invalid to every change class, so a malformed envelope now silently drops out with no operator-visible signal. The set is also a hand-maintained second copy of kernel/runner_authority.py's codes with no sync test. — added 2026-08-04
- [ ] (P3) no API route resolves a package-intake revision id — GET /api/v1/package-intakes is POST-only (405) and nothing enumerates revisions, so after a human registers an intake in the browser a session driving the rest of the lifecycle must ask the human for the id; factory status requires it too. Found WS-P2.36. — added 2026-08-04
- [ ] (P2) The one external observation producer already holds authority its own profile forbids: ORCHESTRATOR_M2M_ROLES maps orchestrator-drift-reporter to 'system' (read live from the production container 2026-08-05), and SYSTEM is the role that drives commands/ready and dispatch. Its registry profile is observe-and-propose and CLAUDE.md says it 'must not be borrowed for canonical mutation' — that is a convention, not an enforcement. Nothing has misused it; the defect is the over-grant. This gates Phase 3 draft exit criterion 3 ('zero automated path exists from any external source to a work-unit state transition — verified by a scan, not a promise'): a SOURCE scan proves the producer's code does not call those routes and cannot prove its credential could not, so the criterion would pass green with the hole open. Fix: bind each producer credential to its permitted endpoint/operation, its declared source_system, and its allowed observation shape, with server-derived actor attribution; prove by negative test that the producer cannot create work units, transition lifecycle state, approve packages, or dispatch. Raised by the Codex Phase-2 review 2026-08-05 as a conditional risk; verified here as already true. — added 2026-08-05
- [ ] (P2) scripts/exit_probe.py (the WS-P2.39 measurement layer, ~29KB) has no tests, so the component that decides pass/fail/not_applicable/unavailable for every wave exit clause does not meet the discrimination bar it exists to enforce. The build session flagged this itself: three of the defects adversarial review found in that change — including two verdict fail-opens (an all-not_applicable run reporting success, and a clause excused while its checks ran and their failure was discarded) — would each have been caught by a single negative control. Add negative controls per probe kind: a probe that would pass on known-bad input is the exact failure this tool exists to prevent, one layer down. — added 2026-08-05
## Future plans

## Known Non-obvious Invariants

- Generic authority approvals satisfy work-unit readiness only. Authority-expanding
  standing-context updates require a named human approval bound to the exact
  standing-context fingerprint.
- Protocol smoke tests may manipulate time or lease expiry as deterministic fixture
  setup. Runtime recovery behavior itself must go through public API/CLI surfaces,
  not private service shortcuts.
- For decomposed units, completion is evaluated only against the approved unit AC
  mapping. Extra package-level adjudications recorded on that unit are ignored by
  the completion guard rather than blocking completion.

## WS-3.1 verification

Persistent orchestrator core is merged and closed. Orchestrator PR #1 merged at
`1ca7090079999dc25441cb0d1066b920b828e271`; intent-package closure PR #9
merged at `473de819ed31a2ab5beadde54dd03c7c71b4c178`. The closed package is
`ws-3.1-orchestrator-core` revision 1 with hash
`4414eae543d9dac8b1983f796593569d9abf97dfee1b8a06ef29b308e7b8337b`.

## WS-3.2 verification

Package intake and decomposition are merged and closed. Orchestrator PR #6
merged at `dd0e3f0deecd12e904b30cb29bfcfc57fb8fd688`; orchestrator
documentation PR #7 merged at `2a73b794665503240e58d12e3df55a8384bbec55`;
intent-package closure PR #10 merged at
`a48a72e10152b08739b3b83d1fba996c203d2f10`. The closed package is
`ws-3.2-package-intake-decomposition` revision 1 with hash
`84c929bc0860b6a585a62ec02fa35d9cdf89fce84773660aea1e383d955689df`.

## WS-3.3 verification

Runtime protocol semantics are merged and closed. Orchestrator PR #9 merged at
`183cbd945ad0dbe871661252cd313d84fd737f22`; intent-package closure PR #11
merged at `61550f21f59b4f70c4f03205e15415bf97cd87fd`. The closed package is
`ws-3.3-protocol-smoke-runtime-semantics` revision 1 with hash
`7829f22bfa30630a906d75131c84bc018c5dac3ceac7b933b7c9b46d23e5047a`.

## Phase 4 bootstrap verification

Option A for the runner reachability gap is approved: deploy the orchestrator to
production before WS-4.1 factory-runner work. Governing package
`orchestrator-production-deploy` revision 1 is approved with hash
`2f6bc7da07aa00106cb6008fc8a85878e001652f6ec645bf25a37760d84c2e7d`.

Repository deploy-prep is merged. Orchestrator PR #14 merged at
`22ce0a9fd4183df1794f0155ec4bd4ba6e4a83b5`; local `main` is clean at that
commit. Verification after merge: `make check` passed with 673 tests; security
scan reported `0 BLOCK`, `0 WARN`, and one judgment-only BWS least-privilege
INFO.

Local dogfooding through the orchestrator completed `deploy-plan`,
`repo-deploy-prep`, and `backup-coverage`. The `backup-coverage` dependency was
satisfied by Devon's explicit uncommon bootstrap waiver only to unblock
infrastructure creation; physical `vps-backup` manifest and restore verification
must still be proven after the production DB exists and before real production
orchestrator data is accepted. `infra-mutation` then ran in a fresh
infrastructure-only session through the change-manager/infraops lane.

`infra-mutation` is complete in the local `orchestrator_runtime` database at
version 11. Production Coolify app `orchestrator` serves `https://sds.alobar.net`;
Alembic is at `0007_work_unit_authority`; health checks pass; the human
surface is protected by Alobar ID forward-auth; M2M auth rejects missing/invalid
credentials and accepted only the configured bootstrap smoke credential during
verification. The bootstrap smoke token was deleted after the test.

The production orchestrator database is now covered by `vps-backup`. The
`vps-backup` repo commit `8ed7586` adds the `orchestrator` dump and verification;
the manifest includes the Coolify Postgres resource and `./verify-backup.sh`
passed with a valid `orchestrator.sql.gz` dump. The durable GitHub-runner M2M
credential was not created in this infra-mutation session; that belongs to
WS-4.1 factory-runner credential rollout through BWS/Coolify-managed secret
references.

## WS-4.1 factory-runner verification

Factory-runner is merged and pilot-ready. `AlobarQuest/factory-runner` PR #1
merged at local merge commit `f0e796f`; `AlobarQuest/orchestrator` PR #16 merged
at local merge commit `03cce5c`. The orchestrator pilot workflow is present but
has not been dispatched.

The durable GitHub-hosted runner M2M credential is active through
BWS/GitHub/Coolify-managed references only. The credential key ID is
`factory-runner-github`; the BWS secret UUID is
`d2a4c0fc-128b-4bf5-8e25-b481010e1be0`; production stores only the token hash in
`ORCHESTRATOR_M2M_CREDENTIALS`.

Production was repaired after WS-4.1 closeout with
`ghcr.io/alobarquest/orchestrator:03cce5c-ws41-closeout`, including the
`factory-runner` registry actor from `security-standards` commit `972c64a`.
Post-rollout smoke checks showed `/health/live` and `/health/ready` returning
200, missing M2M auth returning 401, and the configured key ID plus BWS-backed
bearer returning 200. Devon's merge gate remains permanent; no automatic merge
behavior was added.

## WS-4.2 dispatch-adapter verification

Dispatch adapter implementation is complete locally on branch
`codex/ws42-dispatch-adapter` and pending PR merge. It adds
`dispatch_records`, `POST /api/v1/work-units/{unit_id}/dispatch`,
disabled-by-default runtime dispatch settings, fail-closed admission, idempotent
GitHub Actions `workflow_dispatch`, repeated-failure circuit breaking,
conformance admission, human-gate age-out evidence, and dispatch outcome
events.

Runtime dispatch remains disabled unless explicitly configured through approved
secret/config rollout. No production config was mutated during implementation.
Devon's merge gate remains permanent; no worker or dispatcher may merge PRs.

Verification at implementation closeout:

- `SECURITY_STANDARDS_DIR=/Users/devon/Projects/security-standards make check`
  passed with 698 tests.
- Security scan reported `0 BLOCK`, `0 WARN`, and one judgment-only BWS
  least-privilege INFO.
- `cd /Users/devon/Projects/project-standards && uv run portfolio foundation`
  reported `violations=0 accepted=0 unknown=0`.
- Production `/health/live` and `/health/ready` returned 200.
- Missing M2M auth returned 401.
- Configured WS-4.1 M2M auth returned 200 without printing secret values.
